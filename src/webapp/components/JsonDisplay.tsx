import styled from 'styled-components';

// Parent container for the JsonContainers

const JsonDisplay = styled.div`
  display: flex;
  justify-content: space-between;
  margin: 2rem;
`;

export default JsonDisplay;
